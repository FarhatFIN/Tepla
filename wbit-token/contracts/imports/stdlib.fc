;; Standard library for TON FunC
;; This file is provided by the TON compiler toolchain

forall X -> X car(tuple t) asm "CAR";
forall X -> X cdr(tuple t) asm "CDR";
forall X -> (tuple, X) ~tpop(tuple t) asm "TPOP";

int now() asm "NOW";
slice my_address() asm "MYADDR";
[int, cell] get_balance() asm "BALANCE";
int cur_lt() asm "LTIME";
int block_lt() asm "BLOCKLT";

int cell_hash(cell c) asm "HASHCU";
int slice_hash(slice s) asm "HASHSU";
int string_hash(slice s) asm "SHA256U";

int check_signature(int hash, slice signature, int public_key) asm "CHKSIGNU";
int check_data_signature(slice data, slice signature, int public_key) asm "CHKSIGNS";

(int, int, int) compute_data_size(cell c, int max_cells) impure asm "CDATASIZE";
(int, int, int) slice_compute_data_size(slice s, int max_cells) impure asm "SDATASIZE";

cell begin_cell() asm "NEWC";
builder store_ref(builder b, cell c) asm(c b) "STREF";
builder store_uint(builder b, int x, int len) asm(x b len) "STUX";
builder store_int(builder b, int x, int len) asm(x b len) "STIX";
builder store_slice(builder b, slice s) asm "STSLICER";
builder store_grams(builder b, int x) asm "STGRAMS";
builder store_dict(builder b, cell c) asm(c b) "STDICT";
builder store_maybe_ref(builder b, cell c) asm(c b) "STOPTREF";
cell end_cell(builder b) asm "ENDC";

slice begin_parse(cell c) asm "CTOS";
() end_parse(slice s) impure asm "ENDS";
(slice, cell) load_ref(slice s) asm(-> 1 0) "LDREF";
(slice, int) load_int(slice s, int len) asm(s len -> 1 0) "LDIX";
(slice, int) load_uint(slice s, int len) asm(s len -> 1 0) "LDUX";
(slice, slice) load_bits(slice s, int len) asm(s len -> 1 0) "LDSLICEX";
(slice, int) load_grams(slice s) asm(-> 1 0) "LDGRAMS";
(slice, cell) load_dict(slice s) asm(-> 1 0) "LDDICT";
(slice, cell) load_maybe_ref(slice s) asm(-> 1 0) "LDOPTREF";

cell preload_ref(slice s) asm "PLDREF";
int preload_uint(slice s, int len) asm "PLDUX";

slice preload_bits(slice s, int len) asm "PLDSLICEX";
slice skip_bits(slice s, int len) asm "SDSKIPFIRST";
(slice, ()) ~skip_bits(slice s, int len) asm "SDSKIPFIRST";
slice first_bits(slice s, int len) asm "SDCUTFIRST";

int slice_bits(slice s) asm "SBITS";
int slice_refs(slice s) asm "SREFS";
int slice_empty?(slice s) asm "SEMPTY";
int slice_data_empty?(slice s) asm "SDEMPTY";
int slice_refs_empty?(slice s) asm "SREMPTY";
int slice_depth(slice s) asm "SDEPTH";

int builder_refs(builder b) asm "BREFS";
int builder_bits(builder b) asm "BBITS";
int builder_depth(builder b) asm "BDEPTH";

int cell_depth(cell c) asm "CDEPTH";
int cell_null?(cell c) asm "ISNULL";

builder store_coins(builder b, int x) asm "STVARUINT16";
(slice, int) load_coins(slice s) asm(-> 1 0) "LDVARUINT16";

slice skip_dict(slice s) asm "SKIPDICT";
(slice, int) load_msg_addr(slice s) asm(-> 1 0) "LDMSGADDR";
tuple parse_addr(slice s) asm "PARSEMSGADDR";
(int, int) parse_std_addr(slice s) asm "REWRITESTDADDR";
(int, slice) parse_var_addr(slice s) asm "REWRITEVARADDR";

cell idict_set_ref(cell dict, int key_len, int index, cell value) asm(value index dict key_len) "DICTISETREF";
(cell, ()) ~idict_set_ref(cell dict, int key_len, int index, cell value) asm(value index dict key_len) "DICTISETREF";

(cell, int) idict_get_ref?(cell dict, int key_len, int index) asm(index dict key_len) "DICTIGETOPTREF";
(cell, int) udict_get_ref?(cell dict, int key_len, int index) asm(index dict key_len) "DICTUGETOPTREF";

(cell, cell, int) idict_set_get_ref(cell dict, int key_len, int index, cell value) asm(value index dict key_len) "DICTISETGETOPTREF";
(cell, cell, int) udict_set_get_ref(cell dict, int key_len, int index, cell value) asm(value index dict key_len) "DICTUSETGETOPTREF";

cell idict_get_ref(cell dict, int key_len, int index) asm(index dict key_len) "DICTIGETREF";
cell udict_get_ref(cell dict, int key_len, int index) asm(index dict key_len) "DICTUGETREF";

(cell, int) idict_delete?(cell dict, int key_len, int index) asm(index dict key_len) "DICTIDEL";
(cell, int) udict_delete?(cell dict, int key_len, int index) asm(index dict key_len) "DICTUDEL";

cell new_dict() asm "NEWDICT";
int dict_empty?(cell c) asm "DICTEMPTY";

() set_data(cell c) impure asm "c4 POP";
cell get_data() asm "c4 PUSH";
cont get_c3() impure asm "c3 PUSH";
() set_c3(cont c) impure asm "c3 POP";
() accept_message() impure asm "ACCEPT";
() set_gas_limit(int limit) impure asm "SETGASLIMIT";
() commit() impure asm "COMMIT";
() buy_gas(int gram) impure asm "BUYGAS";

int min(int x, int y) asm "MIN";
int max(int x, int y) asm "MAX";
(int, int) minmax(int x, int y) asm "MINMAX";
int abs(int x) asm "ABS";

() raw_reserve(int amount, int mode) impure asm "RAWRESERVE";
() raw_reserve_extra(int amount, cell extra_amount, int mode) impure asm "RAWRESERVEX";
() send_raw_message(cell msg, int mode) impure asm "SENDRAWMSG";

int equal_slices(slice a, slice b) asm "SDEQ";
int equal_slice_bits(slice a, slice b) asm "SDPFXREV";

() throw(int excno) impure asm "THROW";
() throw_if(int excno, int cond) impure asm "THROWIF";
() throw_unless(int excno, int cond) impure asm "THROWARGIF";

() dump_stack() impure asm "DUMPSTK";

cell config_param(int x) asm "CONFIGOPTPARAM";
int cell_depth(cell c) asm "CDEPTH";

slice null_addr() asm "b{00} PUSHSLICE";
